export interface ITheme{
    primary: string,
    secondary: string,
    backdrop: boolean,
    borders: boolean,
    rounded: boolean,
    opacity: number,
    description: string,
    imgUrl?: string,
    name: string,
}